// 2) Create a function that takes 3 numbers, 
// adds them together, and returns them as a number.

function add3Numbers(x: number, y: number, z: number): number {
    return x + y + z;
}

console.log(add3Numbers(1, 2, 3)); // expecting 6

console.log(1, 2, "yahoo"); // expecting 1 2 yahoo